version: "2"
run:
  tests: false

linters:
  enable:
    - bodyclose
    - durationcheck
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - sloglint
    - unparam
    - unused
    - usestdlibvars
    - usetesting
